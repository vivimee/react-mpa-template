doctype html
html
    head
        meta(charset="utf-8")
        title=$title
        - if ($keywords)
            meta(keywords=$keywords)
        - if ($description)
            meta(description=$description)
        meta(name="viewport" content="width=device-width")
    body
        #react